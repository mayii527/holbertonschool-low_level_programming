#include "holberton.h"
/**
 * mul - Write a function that multiplies two integers.
 * @a: First integer
 * @b: Second integer
 *
 * Return: Returns the result (int).
 */
int mul(int a, int b)
{
return (a * b);
}
